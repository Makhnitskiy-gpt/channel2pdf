# ============================================================================
# КОНФИГУРАЦИЯ ОКРУЖЕНИЯ
# ============================================================================
# Скопируйте этот файл в .env и заполните своими значениями
# cp .env.example .env

# ============================================================================
# ENVIRONMENT (окружение)
# ============================================================================

# Тип окружения: development, production, test
# - development: режим разработки, DEMO_MODE=true по умолчанию
# - production:  боевой режим, DEMO_MODE=false (демо-режим игнорируется)
# - test:        тестовое окружение
ENV=development


# ============================================================================
# ДЕМО-РЕЖИМ
# ============================================================================

# Демо-режим (true/false)
# Логика:
#   - Если ENV=production → ВСЕГДА false (игнорирует эту настройку)
#   - Если ENV=development → по умолчанию true
#   - Если явно задано → использует заданное значение
#
# В демо-режиме:
#   - Используются тестовые данные без подключения к Telegram API
#   - Отображается синяя плашка "Демо-режим" на веб-странице
#   - Поле "Канал" заблокировано (используется demo_channel)
#
# Для ПРОДАКШЕНА ОБЯЗАТЕЛЬНО установите:
#   ENV=production
#   DEMO_MODE=false  (или не указывайте, будет false автоматически)
DEMO_MODE=true


# ============================================================================
# TELEGRAM API
# ============================================================================

# Telegram API credentials
# Получите на: https://my.telegram.org/apps
#
# ВАЖНО ДЛЯ ПРОДАКШЕНА:
#   - Обязательно задайте свои реальные credentials
#   - НЕ используйте примерные значения ниже в боевом окружении
#   - Храните credentials в безопасности (не комммитте в git)

# API ID (целое число)
TELEGRAM_API_ID=28607273

# API HASH (строка из 32 символов)
TELEGRAM_API_HASH=55165d607c1d26dca0d0844d437fde2d


# ============================================================================
# TELEGRAM SESSION
# ============================================================================

# Название файла сессии (без расширения)
# По умолчанию: session (создаст файл session.session)
TELEGRAM_SESSION_NAME=session


# ============================================================================
# ПРИМЕРЫ ДЛЯ РАЗНЫХ ОКРУЖЕНИЙ
# ============================================================================

# ----------------------------------------------------------------------------
# ЛОКАЛЬНАЯ РАЗРАБОТКА (демо-режим)
# ----------------------------------------------------------------------------
# ENV=development
# DEMO_MODE=true
# # API credentials не обязательны в демо-режиме


# ----------------------------------------------------------------------------
# ПРОДАКШЕН (боевой режим)
# ----------------------------------------------------------------------------
# ENV=production
# DEMO_MODE=false
# TELEGRAM_API_ID=your_real_api_id
# TELEGRAM_API_HASH=your_real_api_hash
# TELEGRAM_SESSION_NAME=session


# ----------------------------------------------------------------------------
# STAGING (тестирование с реальными данными)
# ----------------------------------------------------------------------------
# ENV=production
# DEMO_MODE=false
# TELEGRAM_API_ID=staging_api_id
# TELEGRAM_API_HASH=staging_api_hash
# TELEGRAM_SESSION_NAME=staging_session


# ============================================================================
# ПРИМЕЧАНИЯ
# ============================================================================

# 1. Файл .env НЕ должен коммититься в git (уже добавлен в .gitignore)
# 2. Для каждого окружения (dev, prod, staging) создайте свой .env
# 3. В продакшене передавайте переменные через систему деплоя (не файл .env)
# 4. Проверить текущую конфигурацию: python config.py
