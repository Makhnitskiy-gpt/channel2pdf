# Парсер Telegram-каналов

Консольная утилита для парсинга текстовых постов из публичных Telegram-каналов с формированием PDF-отчёта.

## Возможности

- Парсинг постов из публичных Telegram-каналов
- Фильтрация по временному периоду
- Сортировка по дате, количеству реакций или просмотрам
- Генерация PDF-отчёта с форматированием
- Понятные сообщения об ошибках

## Требования

- Python 3.7+
- Telegram API credentials (API_ID и API_HASH) — только для работы с реальными каналами
- Для демо-режима никаких credentials не требуется

## Демо-режим

Если у вас пока нет API_ID и API_HASH для Telegram, вы можете протестировать утилиту в демо-режиме с использованием тестовых данных.

### Что такое демо-режим?

В демо-режиме программа:
- НЕ подключается к Telegram API
- Работает с заранее подготовленными тестовыми постами (7 постов с различными комбинациями данных)
- Позволяет протестировать CLI-интерфейс, сортировку и генерацию PDF
- Использует введённый диапазон дат для фильтрации тестовых данных

### Как включить демо-режим?

Демо-режим активируется автоматически, если выполняется хотя бы одно из условий:

1. `DEMO_MODE = True` в [config.py](config.py)
2. `API_ID` не задан (None или 0)
3. `API_HASH` не задан (None или пустая строка)

По умолчанию в [config.py](config.py) установлено `DEMO_MODE = True`.

### Как запустить в демо-режиме?

```bash
# Установите зависимости
pip install -r requirements.txt

# Запустите программу
python main.py
```

При запуске вы увидите предупреждение о демо-режиме:

```
============================================================
Парсер Telegram-каналов
============================================================

⚠️  ДЕМО-РЕЖИМ
============================================================
API_ID или API_HASH не заданы в config.py
Программа работает с тестовыми данными без подключения к Telegram.
Ввод канала будет пропущен.
============================================================
```

Далее программа попросит ввести:
- Дата начала периода
- Дата конца периода
- Тип сортировки
- Направление сортировки
- Имя PDF-файла (опционально)

### Тестовые данные

В демо-режиме генерируется 7 тестовых постов:

1. **Пост 1**: короткий текст, есть реакции (3 вида) и просмотры
2. **Пост 2**: длинный многострочный текст, есть реакции и просмотры
3. **Пост 3**: средний текст, только просмотры (без реакций)
4. **Пост 4**: средний текст, только реакции (без просмотров)
5. **Пост 5**: минималистичный — только текст и дата
6. **Пост 6**: много реакций, мало просмотров
7. **Пост 7**: очень длинный текст (5+ абзацев), средние показатели

Это позволяет протестировать все варианты отображения в PDF.

### Переход на реальные данные

Когда получите Telegram API credentials:

1. Откройте [config.py](config.py)
2. Заполните `API_ID` и `API_HASH`
3. Установите `DEMO_MODE = False`
4. Запустите программу — теперь она будет работать с реальными каналами

## Установка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Получите Telegram API credentials

1. Перейдите на https://my.telegram.org/apps
2. Войдите в свой аккаунт Telegram
3. Создайте новое приложение
4. Скопируйте `API_ID` и `API_HASH`

### 3. Настройте конфигурацию

Откройте файл `config.py` и заполните значения:

```python
API_ID = 12345678  # Ваш API_ID
API_HASH = "your_api_hash_here"  # Ваш API_HASH
SESSION_NAME = "session"  # Можно не менять
```

## Использование

Запустите программу:

```bash
python main.py
```

Программа попросит ввести следующие данные:

### 1. Username или ссылка на канал

Примеры:
- `@channelname`
- `channelname`
- `https://t.me/channelname`

### 2. Дата начала периода

Формат: `YYYY-MM-DD`

Пример: `2024-01-01`

### 3. Дата конца периода

Формат: `YYYY-MM-DD`

Пример: `2024-03-31`

### 4. Тип сортировки

Выберите один из вариантов:
- `1` — по дате
- `2` — по количеству реакций (сумма всех реакций)
- `3` — по количеству просмотров

### 5. Направление сортировки

Выберите один из вариантов:
- `1` — по возрастанию
- `2` — по убыванию

### 6. Имя PDF-файла (необязательно)

Если не указать, будет сгенерировано автоматически:
`channelname_2024-01-01_2024-03-31.pdf`

## Пример использования

```
============================================================
Парсер Telegram-каналов
============================================================

Введите username или ссылку на канал (например, @channelname): @durov
Введите дату начала периода (YYYY-MM-DD): 2024-01-01
Введите дату конца периода (YYYY-MM-DD): 2024-03-31

Выберите тип сортировки:
  1 - по дате
  2 - по количеству реакций
  3 - по количеству просмотров
Введите номер (1-3): 2

Выберите направление сортировки:
  1 - по возрастанию
  2 - по убыванию
Введите номер (1-2): 2

Введите имя PDF-файла (Enter для 'durov_2024-01-01_2024-03-31.pdf'):

============================================================
Подключение к Telegram...
Подключение успешно!

Получение постов из канала...
Канал: @durov
Период: с 2024-01-01 по 2024-03-31

Найдено постов: 15

Сортировка по количеству реакций (по убыванию)...
Создание PDF-файла: durov_2024-01-01_2024-03-31.pdf...

============================================================
Готово!
PDF-файл создан: /Users/username/durov_2024-01-01_2024-03-31.pdf
============================================================
```

## Формат PDF-отчёта

Каждый пост в PDF содержит:

### Шапка поста
- Дата в формате ДД.ММ.ГГГГ: `[05.03.2024]`
- Топ-3 реакции (если есть): `❤️ 120, 👍 45, 😂 33`
- Просмотры (если есть): `👁 5678`
- Разделитель между элементами: ` / `

Примеры:
- С реакциями и просмотрами: `[05.03.2024] / ❤️ 120, 👍 45 / 👁 5678`
- Без реакций: `[05.03.2024] / 👁 5678`
- Без реакций и просмотров: `[05.03.2024]`

### Текст поста
Полный текст поста с сохранением переносов строк.

### Разделитель
Горизонтальная линия между постами.

## Обработка ошибок

Программа корректно обрабатывает следующие ситуации:

- **API credentials не заполнены**: выводится подсказка с инструкцией
- **Неверный формат даты**: запрос на повторный ввод
- **Канал не найден**: сообщение об ошибке
- **Приватный канал**: сообщение о недоступности
- **Постов не найдено**: информационное сообщение
- **Ctrl+C**: корректное завершение программы

## Веб-версия

Помимо консольной версии, проект имеет веб-интерфейс на FastAPI с удобной формой для генерации отчётов.

### Запуск веб-сервера

1. Установите зависимости (если ещё не установлены):

```bash
pip install -r requirements.txt
```

2. Запустите сервер:

```bash
uvicorn web_app:app --reload
```

Или:

```bash
python web_app.py
```

3. Откройте в браузере:

```
http://localhost:8000
```

### Как пользоваться веб-версией

1. Откройте главную страницу в браузере
2. Заполните форму:
   - **Канал**: username канала (@channelname) или ссылка (https://t.me/channelname)
   - **Дата начала**: дата начала периода (YYYY-MM-DD)
   - **Дата окончания**: дата окончания периода (YYYY-MM-DD)
   - **Тип сортировки**: по дате / реакциям / просмотрам
   - **Направление**: по возрастанию / убыванию
   - **Имя файла** (опционально): название PDF-файла
3. Нажмите кнопку "Собрать PDF"
4. Дождитесь завершения генерации
5. Скачайте готовый PDF-файл

### API эндпоинты

- `GET /` - главная страница с формой
- `POST /generate` - генерация отчёта
- `GET /files/{filename}` - скачивание сгенерированного PDF
- `GET /health` - healthcheck (возвращает `{"status": "ok"}`)

### Особенности веб-версии

- **Безопасность**: Telegram API ключи остаются только на сервере, не передаются на клиент
- **Валидация**: все параметры валидируются на стороне сервера
- **Обработка ошибок**: понятные сообщения об ошибках в интерфейсе
- **Сохранение значений**: при ошибке значения полей формы сохраняются
- **Демо-режим**: работает так же, как и в CLI-версии
- **Защита файлов**: невозможно скачать файлы вне директории `generated/`

### Развёртывание в продакшн

Для локального продакшн-окружения рекомендуется:

```bash
# С указанием host и порта
uvicorn web_app:app --host 0.0.0.0 --port 8000

# Или с несколькими воркерами
uvicorn web_app:app --host 0.0.0.0 --port 8000 --workers 4
```

Также рекомендуется использовать reverse proxy (nginx/caddy) и HTTPS.

Для деплоя на облачные платформы см. раздел [Deploy to Render.com](#deploy-to-rendercom) ниже.

---

## Deploy to Render.com

Полное руководство по развёртыванию **Channel2PDF** на платформе Render.com.

### Требования

Перед началом убедитесь, что у вас есть:

1. ✅ **Аккаунт на [Render.com](https://render.com)**
   - Зарегистрируйтесь через GitHub для упрощённой интеграции
   - Бесплатный план (Free) подходит для старта

2. ✅ **GitHub-репозиторий с проектом**
   - Код должен быть в репозитории на GitHub
   - Репозиторий может быть публичным или приватным

3. ✅ **Telegram API credentials**
   - **API_ID** и **API_HASH** из [my.telegram.org/apps](https://my.telegram.org/apps)
   - Инструкция по получению см. в разделе [Установка](#установка)

---

### Шаг 1: Подготовка кода

Убедитесь, что ваш код готов к продакшену:

```bash
# Проверьте, что все файлы на месте
ls -la render.yaml requirements.txt web_app.py

# Закоммитьте последние изменения
git add .
git commit -m "Prepare Channel2PDF for production deployment"
git push origin main
```

**Важные файлы:**
- ✅ [render.yaml](render.yaml) — конфигурация для автоматического деплоя
- ✅ [requirements.txt](requirements.txt) — список Python-зависимостей
- ✅ [web_app.py](web_app.py) — FastAPI приложение (entrypoint)

---

### Шаг 2: Создание Web Service на Render

1. **Войдите в [Render Dashboard](https://dashboard.render.com)**

2. **Нажмите "New +" → "Web Service"**

3. **Подключите GitHub-репозиторий:**
   - Выберите репозиторий с проектом Channel2PDF
   - Если это первый раз, дайте Render доступ к вашим репозиториям

4. **Render автоматически обнаружит `render.yaml`:**
   - Вы увидите сообщение: _"Blueprint detected"_
   - Render предложит создать сервис на основе конфигурации

5. **Выберите регион (Region):**
   - Рекомендуется: **Oregon (US West)** или **Frankfurt (EU Central)**
   - Выбирайте ближайший к вашей целевой аудитории

6. **Нажмите "Apply"** для создания сервиса

---

### Шаг 3: Настройка переменных окружения

После создания сервиса перейдите в **Environment** и добавьте следующие переменные:

| ENV-переменная | Обязательно? | Описание |
|----------------|--------------|----------|
| `ENV` | ✅ Да | Устанавливается автоматически в `production` через `render.yaml` |
| `TELEGRAM_API_ID` | ✅ Да | API ID из [my.telegram.org/apps](https://my.telegram.org/apps) |
| `TELEGRAM_API_HASH` | ✅ Да | API Hash из [my.telegram.org/apps](https://my.telegram.org/apps) |
| `DEMO_MODE` | ⚠️ Опционально | `false` для прода (по умолчанию определяется автоматически) |
| `TELEGRAM_SESSION_NAME` | ⚠️ Опционально | Имя сессии Telegram (по умолчанию: `session`) |
| `ANALYTICS_SNIPPET` | ❌ Нет | HTML-код внешнего счётчика (Яндекс.Метрика, GA4, Plausible) |

**Как добавить переменные:**

1. В Render Dashboard откройте ваш сервис
2. Перейдите в раздел **Environment** (левое меню)
3. Нажмите **Add Environment Variable**
4. Введите:
   - **Key**: `TELEGRAM_API_ID`
   - **Value**: ваш API ID (например: `12345678`)
5. Нажмите **Add**
6. Повторите для `TELEGRAM_API_HASH`

**⚠️ Важно:**
- `ENV=production` уже установлен через `render.yaml`
- Без `TELEGRAM_API_ID` и `TELEGRAM_API_HASH` сервис будет работать в демо-режиме
- Демо-режим подходит для тестирования, но не для реальной работы

---

### Шаг 4: Первый деплой

1. **Render автоматически запустит билд:**
   - Установит зависимости из `requirements.txt`
   - Запустит сервер через `uvicorn web_app:app`

2. **Следите за логами сборки:**
   - В разделе **Logs** вы увидите процесс установки зависимостей
   - Билд занимает ~2-5 минут (бесплатный план медленнее)

3. **Дождитесь статуса "Live":**
   - Когда сервис запустится, статус изменится на зелёный
   - URL будет вида: `https://channel2pdf.onrender.com`

---

### Шаг 5: Проверка после деплоя

После успешного запуска выполните следующие проверки:

#### ✅ 1. Откройте главную страницу

```
https://your-service-name.onrender.com
```

**Что проверить:**
- Страница загружается корректно
- Видна форма генерации PDF
- Переключатель языков (RU/EN) работает
- Логотип и фавиконка отображаются
- Тёмная тема применяется

#### ✅ 2. Проверьте healthcheck

```
https://your-service-name.onrender.com/health
```

**Ожидаемый ответ:**
```json
{"status": "ok"}
```

#### ✅ 3. Протестируйте генерацию PDF

1. Заполните форму:
   - Канал: `@durov` (или любой публичный канал)
   - Даты: выберите диапазон (например, за последний месяц)
   - Сортировка: по дате или реакциям
2. Нажмите **"Собрать PDF"**
3. Дождитесь генерации (может занять 10-60 секунд в зависимости от канала)
4. Скачайте готовый PDF

#### ✅ 4. Проверьте логи

В Render Dashboard → **Logs**:

**Что искать:**
```
✅ INFO: Started server process
✅ INFO: Application startup complete
✅ Uvicorn running on http://0.0.0.0:XXXX
✅ {"timestamp": "...", "event_type": "export_started", ...}
✅ {"timestamp": "...", "event_type": "export_success", ...}
```

**Если есть ошибки:**
- Проверьте переменные окружения (особенно `TELEGRAM_API_ID`)
- Убедитесь, что API credentials корректны
- Проверьте, что канал публичный и доступен

#### ✅ 5. Проверьте превью ссылки (Open Graph)

**Telegram:**
1. Отправьте ссылку на сервис в Telegram
2. Убедитесь, что отображается превью с логотипом и описанием

**Facebook/LinkedIn:**
1. Используйте [Facebook Sharing Debugger](https://developers.facebook.com/tools/debug/)
2. Вставьте URL вашего сервиса
3. Проверьте превью OG-изображения

---

### Шаг 6: Настройка кастомного домена (опционально)

Если у вас есть свой домен (например, `channel2pdf.com`):

1. **В Render Dashboard:**
   - Откройте ваш сервис
   - Перейдите в **Settings** → **Custom Domain**
   - Нажмите **Add Custom Domain**
   - Введите домен: `channel2pdf.com` или `app.channel2pdf.com`

2. **В настройках DNS у регистратора:**
   - Добавьте CNAME-запись:
     ```
     app.channel2pdf.com → your-service-name.onrender.com
     ```
   - Или A-запись с IP-адресом от Render (указан в инструкции)

3. **Дождитесь проверки DNS:**
   - Занимает 5-60 минут
   - Render автоматически выпустит SSL-сертификат (Let's Encrypt)

4. **Обновите SEO-мета теги в коде:**
   - В [templates/index.html](templates/index.html#L25):
     ```html
     <meta property="og:url" content="https://channel2pdf.com">
     ```
   - В JSON-LD структуре:
     ```json
     "url": "https://channel2pdf.com"
     ```
   - Закоммитьте и запушьте изменения

---

### Особенности Render Free Plan

**Бесплатный план включает:**
- ✅ 750 часов работы сервиса в месяц
- ✅ Автоматический SSL (HTTPS)
- ✅ Автоматические редеплои при push в GitHub
- ✅ Базовая защита от DDoS

**Ограничения:**
- ⚠️ **Сервис "засыпает" через 15 минут неактивности**
  - Первый запрос после сна занимает 30-60 секунд (cold start)
  - Решение: используйте [uptimerobot.com](https://uptimerobot.com) для пингов каждые 5 минут
- ⚠️ **512 MB RAM** — достаточно для Channel2PDF, но при больших каналах (>1000 постов) может потребоваться платный план
- ⚠️ **Shared CPU** — генерация PDF может быть медленнее, чем локально

---

### Мониторинг и обслуживание

#### Просмотр логов

```bash
# В реальном времени через Render Dashboard
# Или через Render CLI (если установлен):
render logs -s your-service-name
```

#### Аналитика использования

1. **Встроенная аналитика:**
   - URL: `https://your-service-name.onrender.com/admin/analytics`
   - ⚠️ Доступна только в dev-режиме (в проде отключена)
   - Для доступа в проде добавьте HTTP basic auth (см. [ANALYTICS.md](ANALYTICS.md))

2. **CLI-аналитика:**
   ```bash
   # Скачайте логи из Render и проанализируйте локально
   python analyze_logs.py
   ```

3. **Внешние счётчики:**
   - Настройте Яндекс.Метрику, Google Analytics или Plausible
   - Добавьте код счётчика через `ANALYTICS_SNIPPET` (см. [ANALYTICS.md](ANALYTICS.md))

#### Обновление кода

```bash
# Локально
git add .
git commit -m "Update feature X"
git push origin main

# Render автоматически запустит редеплой
# Следите за прогрессом в Dashboard → Logs
```

#### Откат к предыдущей версии

В Render Dashboard:
1. Перейдите в **Events**
2. Найдите нужный деплой
3. Нажмите **Rollback to this version**

---

### Устранение проблем (Troubleshooting)

#### Сервис не запускается

**Проблема:** Build failed или Deploy failed

**Решение:**
1. Проверьте логи сборки в Render Dashboard
2. Убедитесь, что `requirements.txt` корректен:
   ```bash
   pip install -r requirements.txt
   ```
3. Проверьте синтаксис в `render.yaml`
4. Убедитесь, что в `web_app.py` есть:
   ```python
   app = FastAPI(...)
   ```

#### Healthcheck не проходит

**Проблема:** Render показывает "Unhealthy"

**Решение:**
1. Проверьте эндпоинт `/health`:
   ```bash
   curl https://your-service-name.onrender.com/health
   ```
2. Убедитесь, что возвращается `{"status": "ok"}`
3. Проверьте, что сервер слушает на `0.0.0.0` и `$PORT`

#### PDF не генерируется

**Проблема:** Ошибка при генерации PDF

**Решение:**
1. Проверьте переменные `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
2. Убедитесь, что канал публичный
3. Проверьте лимиты API (Telegram ограничивает частые запросы)
4. Увеличьте таймаут в Render (Settings → Timeout)

#### Медленная работа

**Проблема:** Генерация PDF занимает >2 минуты

**Решение:**
1. Уменьшите диапазон дат (меньше постов)
2. Рассмотрите платный план Render (больше CPU/RAM)
3. Оптимизируйте код (кэширование, параллелизм)

---

### Стоимость и апгрейд

**Бесплатный план (Free):**
- Подходит для тестирования и небольших нагрузок
- Ограничения: засыпание, shared CPU, 512 MB RAM

**Платный план (Starter: $7/мес):**
- ✅ Нет засыпания
- ✅ Dedicated CPU
- ✅ 512 MB RAM (можно увеличить)
- ✅ Приоритетная поддержка

**Когда апгрейдить:**
- Если сервис используется активно (cold start мешает)
- Если нужно обрабатывать большие каналы (>1000 постов)
- Если требуется стабильное время ответа

---

### Дополнительные ресурсы

- 📘 [Render Documentation](https://docs.render.com/)
- 📘 [FastAPI Deployment Guide](https://fastapi.tiangolo.com/deployment/)
- 📘 [ANALYTICS.md](ANALYTICS.md) — встроенная аналитика Channel2PDF
- 📘 [config.py](config.py) — конфигурация переменных окружения
- 🎥 [Render YouTube Tutorials](https://www.youtube.com/c/Render)

---

## Структура проекта

```
telegram-parser/
├── main.py                 # CLI-интерфейс
├── web_app.py              # Веб-интерфейс (FastAPI)
├── core_service.py         # Бизнес-логика (общая для CLI и веб)
├── telegram_client.py      # Работа с Telegram API
├── fetch_posts.py          # Парсинг и сортировка постов
├── pdf_builder.py          # Генерация PDF
├── config.py               # Конфигурация API
├── requirements.txt        # Зависимости
├── README.md              # Документация
├── templates/              # HTML-шаблоны для веб-версии
│   └── index.html
├── static/                 # Статические файлы (CSS)
│   └── styles.css
└── generated/              # Директория для сгенерированных PDF (веб-версия)
```

## Особенности реализации

### Фильтрация постов
- Включаются только посты с текстом
- Посты без текста пропускаются

### Реакции
- Показываются топ-3 самые популярные реакции
- Сортируются по количеству (по убыванию)
- Если реакций нет, блок не отображается

### Просмотры
- Если статистика просмотров недоступна, считается как 0 при сортировке
- В PDF блок просмотров не отображается, если просмотров нет

### Шрифты в PDF
- Приоритет: Times New Roman (Windows)
- Альтернатива: Liberation Serif (Linux/macOS)
- Запасной вариант: Helvetica (встроенный)

## Лицензия

Свободное использование.

## Поддержка

При возникновении проблем проверьте:
1. Правильность API_ID и API_HASH в config.py
2. Доступность канала (публичный или нет)
3. Правильность формата дат
4. Наличие интернет-соединения

При первом запуске Telegram попросит ввести номер телефона и код подтверждения для авторизации.
