# 🚀 Запуск веб-сервера FastAPI

## ✅ Быстрый запуск (если venv уже создан)

Выполните эти команды **в точном порядке**:

```bash
# 1. Перейдите в директорию проекта
cd "/Users/danil/VS Code"

# 2. Активируйте виртуальное окружение
source venv/bin/activate

# 3. Запустите сервер
uvicorn web_app:app --reload
```

После запуска откройте в браузере: **http://localhost:8000**

---

## 📦 Первый запуск (если venv ещё не создан)

Если вы запускаете проект впервые или удалили папку `venv`:

```bash
# 1. Перейдите в директорию проекта
cd "/Users/danil/VS Code"

# 2. Создайте виртуальное окружение
python3 -m venv venv

# 3. Активируйте его
source venv/bin/activate

# 4. Установите зависимости
pip install -r requirements.txt

# 5. Запустите сервер
uvicorn web_app:app --reload
```

---

## 🎬 Альтернативный способ (через скрипт)

Можно запустить через готовый скрипт:

```bash
cd "/Users/danil/VS Code"
./start_web.sh
```

---

## 🛑 Остановка сервера

Для остановки сервера нажмите `Ctrl+C` в терминале.

---

## 📋 Проверка статуса

После запуска сервера проверьте его работу:

```bash
# В новом окне терминала
curl http://localhost:8000/health
```

Должно вернуться: `{"status":"ok"}`

---

## ⚙️ Настройка (опционально)

### Работа с реальными каналами

По умолчанию проект работает в демо-режиме с тестовыми данными.

Для работы с реальными Telegram-каналами:

1. Откройте файл `config.py`
2. Получите API credentials на https://my.telegram.org/apps
3. Заполните `API_ID` и `API_HASH`
4. Установите `DEMO_MODE = False`

### Изменение порта

Если порт 8000 занят, запустите на другом порту:

```bash
uvicorn web_app:app --reload --port 8080
```

Тогда открывайте: http://localhost:8080

---

## 🐛 Решение проблем

### Ошибка "command not found: uvicorn"

**Причина:** Виртуальное окружение не активировано.

**Решение:**
```bash
source venv/bin/activate
```

### Ошибка "Address already in use"

**Причина:** Порт 8000 занят другим процессом.

**Решение 1:** Освободите порт
```bash
lsof -ti:8000 | xargs kill -9
```

**Решение 2:** Используйте другой порт
```bash
uvicorn web_app:app --reload --port 8080
```

### Ошибка "No module named 'fastapi'"

**Причина:** Зависимости не установлены в venv.

**Решение:**
```bash
source venv/bin/activate
pip install -r requirements.txt
```

---

## 📁 Структура проекта

```
/Users/danil/VS Code/
├── web_app.py              # ← Главный файл веб-приложения
├── core_service.py         # Бизнес-логика
├── main.py                 # CLI-версия
├── config.py               # Настройки
├── requirements.txt        # Зависимости
├── start_web.sh           # Скрипт запуска
├── templates/             # HTML-шаблоны
│   └── index.html
├── static/                # CSS-файлы
│   └── styles.css
├── generated/             # Сгенерированные PDF
└── venv/                  # Виртуальное окружение
```

---

## ✨ Проверка работоспособности

После запуска сервера проверьте:

1. **Healthcheck:**
   ```bash
   curl http://localhost:8000/health
   ```
   Ответ: `{"status":"ok"}`

2. **Главная страница:**
   Откройте в браузере: http://localhost:8000

3. **Генерация PDF (демо-режим):**
   - Заполните форму
   - Нажмите "Собрать PDF"
   - Скачайте готовый файл

---

## 💡 Полезные команды

```bash
# Деактивация venv (когда закончили работу)
deactivate

# Проверка версии Python
python3 --version

# Список установленных пакетов
pip list

# Обновление зависимостей
pip install --upgrade -r requirements.txt

# Просмотр логов сервера в реальном времени
# (логи выводятся в консоль при запуске с --reload)
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Убедитесь, что Python 3.7+ установлен: `python3 --version`
2. Убедитесь, что venv активирован: должно быть `(venv)` в начале строки терминала
3. Убедитесь, что все зависимости установлены: `pip list | grep fastapi`
4. Проверьте логи в консоли при запуске сервера

---

**🎉 Готово! Ваш веб-сервер должен успешно запуститься!**
