# Руководство по конфигурации окружений

## 🎯 Проблема и решение

### Была проблема

На продакшен-домене приложение работало в демо-режиме:
- Отображалась синяя плашка "Демо-режим"
- Использовались тестовые данные вместо реальных Telegram каналов
- Поле "Канал" было заблокировано

### Решение

Создана система управления окружениями через переменные окружения с автоматическим определением режима работы.

---

## 📋 Как работает конфигурация

### Логика определения демо-режима

```
ЕСЛИ задана переменная DEMO_MODE:
    → используется заданное значение

ИНАЧЕ ЕСЛИ ENV=production:
    → DEMO_MODE = False (ВСЕГДА!)

ИНАЧЕ (ENV=development или не задан):
    → DEMO_MODE = True (по умолчанию для удобства разработки)
```

### Ключевые переменные

| Переменная | Описание | Значения | Обязательна? |
|------------|----------|----------|--------------|
| `ENV` | Тип окружения | `development`, `production`, `test` | Нет (по умолчанию `development`) |
| `DEMO_MODE` | Демо-режим | `true`, `false` | Нет (зависит от `ENV`) |
| `TELEGRAM_API_ID` | API ID от Telegram | Целое число | Да (для боевого режима) |
| `TELEGRAM_API_HASH` | API Hash от Telegram | Строка 32 символа | Да (для боевого режима) |
| `TELEGRAM_SESSION_NAME` | Имя файла сессии | Строка | Нет (по умолчанию `session`) |

---

## 🚀 Настройка для ПРОДАКШЕНА

### Вариант 1: Через переменные окружения (рекомендуется)

Установите переменные окружения на вашем хостинге:

```bash
ENV=production
DEMO_MODE=false  # Опционально, т.к. ENV=production автоматически отключает демо-режим
TELEGRAM_API_ID=your_real_api_id
TELEGRAM_API_HASH=your_real_api_hash
```

### Вариант 2: Через файл .env (не рекомендуется для продакшена)

Создайте файл `.env` на сервере:

```bash
cp .env.example .env
nano .env
```

Содержимое `.env`:

```bash
ENV=production
DEMO_MODE=false
TELEGRAM_API_ID=your_real_api_id
TELEGRAM_API_HASH=your_real_api_hash
```

**⚠️ ВНИМАНИЕ:** Файл `.env` не должен коммититься в git! Он уже добавлен в `.gitignore`.

### Проверка конфигурации

После настройки переменных окружения проверьте конфигурацию:

```bash
python3 config.py
```

Ожидаемый вывод для продакшена:

```
🚀 Продакшен режим: DEMO_MODE = False

============================================================
📋 КОНФИГУРАЦИЯ ПРИЛОЖЕНИЯ
============================================================
Окружение:          production
Демо-режим:         False
API ID:             [задан]
API HASH:           [задан]
Session файл:       session
============================================================
```

### Запуск в продакшене

```bash
# Убедитесь, что переменные окружения установлены
export ENV=production
export TELEGRAM_API_ID=your_api_id
export TELEGRAM_API_HASH=your_api_hash

# Запустите сервер
./start_web.sh

# Или вручную
uvicorn web_app:app --host 0.0.0.0 --port 8000
```

---

## 🔧 Настройка для ЛОКАЛЬНОЙ РАЗРАБОТКИ

### По умолчанию (ничего не нужно настраивать)

Проект работает в демо-режиме из коробки:

```bash
# Просто запустите
./start_web.sh
```

Автоматически:
- `ENV=development`
- `DEMO_MODE=true`
- Используются тестовые данные

### С файлом .env (опционально)

Создайте `.env` для переопределения настроек:

```bash
cp .env.example .env
nano .env
```

Пример `.env` для разработки:

```bash
ENV=development
DEMO_MODE=true
# API credentials не обязательны в демо-режиме
```

---

## 🧪 Настройка для ТЕСТИРОВАНИЯ

### Демо-режим с валидацией

```bash
ENV=test
DEMO_MODE=true
```

### Тестирование с реальными данными

```bash
ENV=test
DEMO_MODE=false
TELEGRAM_API_ID=test_api_id
TELEGRAM_API_HASH=test_api_hash
TELEGRAM_SESSION_NAME=test_session
```

---

## 📝 Примеры использования

### Проверка текущей конфигурации

```bash
# Покажет текущие настройки
python3 config.py
```

### Тестирование разных режимов

```bash
# Демо-режим (development)
python3 config.py
# → DEMO_MODE = True

# Продакшен режим
ENV=production python3 config.py
# → DEMO_MODE = False

# Явное указание демо-режима
ENV=development DEMO_MODE=false python3 config.py
# → DEMO_MODE = False (даже в development)
```

### Запуск веб-сервера в разных режимах

```bash
# Демо-режим (по умолчанию)
./start_web.sh

# Продакшен режим
ENV=production ./start_web.sh

# Продакшен с явными credentials
ENV=production \
TELEGRAM_API_ID=123456 \
TELEGRAM_API_HASH=abcdef123456 \
./start_web.sh
```

---

## 🛠️ Файлы конфигурации

### config.py

Основной файл конфигурации с:
- Загрузкой переменных окружения из `.env`
- Логикой определения демо-режима
- Валидацией настроек
- Предупреждениями о неправильной конфигурации

### .env.example

Шаблон для создания `.env` файла:

```bash
cp .env.example .env
```

Содержит примеры для всех окружений с подробными комментариями.

### .env

**Ваш локальный файл с настройками (не коммитится в git):**

1. Создайте из примера: `cp .env.example .env`
2. Отредактируйте под свои нужды
3. Файл автоматически загрузится при старте приложения

---

## ⚠️ Важные замечания

### Безопасность

1. **НЕ коммитьте** файл `.env` в git
2. **НЕ храните** реальные credentials в коде
3. **Используйте** переменные окружения для чувствительных данных
4. **Проверяйте** `.gitignore` - `.env` уже добавлен

### Продакшен

1. **ВСЕГДА** устанавливайте `ENV=production`
2. **ОБЯЗАТЕЛЬНО** задайте свои `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
3. **НИКОГДА** не используйте демо-режим в продакшене
4. **Проверяйте** конфигурацию перед деплоем: `python3 config.py`

### Демо-режим

В демо-режиме (`DEMO_MODE=true`):
- ✅ Используются тестовые данные
- ✅ Не требуется Telegram API
- ✅ Отображается синяя плашка на веб-странице
- ✅ Поле "Канал" заблокировано
- ❌ Нельзя работать с реальными каналами

В боевом режиме (`DEMO_MODE=false`):
- ✅ Работа с реальными Telegram каналами
- ✅ Требуются API credentials
- ✅ Нет демо-плашки на веб-странице
- ✅ Поле "Канал" активно
- ❌ Не работает без API credentials

---

## 🔍 Диагностика проблем

### Проблема: "Демо-режим активен в продакшене"

**Симптомы:**
- На продакшен-сайте отображается синяя плашка "Демо-режим"
- Используются тестовые данные

**Решение:**

```bash
# 1. Проверьте переменные окружения
env | grep ENV
env | grep DEMO_MODE

# 2. Установите правильные значения
export ENV=production
export DEMO_MODE=false  # Опционально

# 3. Перезапустите приложение
pkill -f uvicorn
./start_web.sh

# 4. Проверьте конфигурацию
python3 config.py
```

### Проблема: "API credentials не работают"

**Симптомы:**
- Ошибки подключения к Telegram
- "Unauthorized" errors

**Решение:**

```bash
# 1. Проверьте, что credentials установлены
python3 -c "import config; print(f'API_ID: {config.API_ID}, API_HASH: {config.API_HASH[:5]}...')"

# 2. Убедитесь, что демо-режим отключён
python3 config.py | grep "Демо-режим"

# 3. Проверьте переменные окружения
echo $TELEGRAM_API_ID
echo $TELEGRAM_API_HASH
```

### Проблема: ".env файл не загружается"

**Симптомы:**
- Переменные из `.env` игнорируются
- Используются значения по умолчанию

**Решение:**

```bash
# 1. Проверьте наличие файла
ls -la .env

# 2. Проверьте содержимое
cat .env

# 3. Убедитесь, что python-dotenv установлен
pip list | grep python-dotenv

# 4. Переустановите если нужно
pip install python-dotenv==1.2.1
```

---

## 📚 Дополнительные ресурсы

- [.env.example](.env.example) - шаблон конфигурации
- [config.py](config.py) - исходный код конфигурации
- [QUICK_RUN.md](QUICK_RUN.md) - быстрый старт
- [WEB_SERVER_GUIDE.md](WEB_SERVER_GUIDE.md) - руководство по веб-серверу

---

## ✅ Чек-лист для продакшена

Перед деплоем убедитесь:

- [ ] `ENV=production` установлен
- [ ] `DEMO_MODE=false` (или не задан - будет false автоматически)
- [ ] `TELEGRAM_API_ID` задан (ваш реальный API ID)
- [ ] `TELEGRAM_API_HASH` задан (ваш реальный API Hash)
- [ ] Файл `.env` НЕ коммитится в git
- [ ] `python3 config.py` показывает корректные настройки
- [ ] Веб-страница НЕ показывает демо-плашку
- [ ] Поле "Канал" активно и можно вводить данные

---

## 🎓 Итоговая формула

### Когда включается демо-режим?

```python
if DEMO_MODE is explicitly set:
    use DEMO_MODE value
elif ENV == 'production':
    DEMO_MODE = False  # ВСЕГДА!
else:
    DEMO_MODE = True   # development по умолчанию
```

### Что нужно для продакшена?

```bash
# МИНИМУМ (рекомендуется):
ENV=production
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash

# DEMO_MODE автоматически будет False
```

---

**Готово к использованию!** 🎉

Для любых вопросов: проверьте `python3 config.py` или откройте issue.
