# Руководство по запуску веб-сервера

## 🚀 Быстрый старт

### 1. Запуск веб-сервера

```bash
# Вариант 1: Использование скрипта
./start_web.sh

# Вариант 2: Ручной запуск
source venv/bin/activate
uvicorn web_app:app --reload --host 127.0.0.1 --port 8000
```

Сервер запустится на: **http://127.0.0.1:8000**

### 2. Остановка сервера

```bash
# Найти и убить процесс
pkill -f "uvicorn web_app:app"

# Или использовать Ctrl+C в терминале, где запущен сервер
```

---

## 🧪 Тестирование

### Тест веб-интерфейса с эмодзи

```bash
# Активировать окружение
source venv/bin/activate

# Запустить тест
python3 test_web_emoji.py
```

**Что проверяет тест:**
- ✅ Доступность веб-сервера
- ✅ Генерацию PDF через API
- ✅ Корректность обработки эмодзи

### Демо-режим

Для быстрого тестирования без Telegram API установите в [config.py](config.py):

```python
DEMO_MODE = True
```

В демо-режиме:
- Не требуется подключение к Telegram
- Используются тестовые данные с эмодзи
- Все функции работают как в продакшене

---

## 🌐 Использование веб-интерфейса

### Шаг 1: Откройте браузер

Перейдите на: **http://127.0.0.1:8000**

### Шаг 2: Заполните форму

**Обязательные поля:**

1. **Канал** - username канала (без @)
   - Пример: `durov`
   - В демо-режиме: любое значение

2. **Дата начала** - формат ГГГГ-ММ-ДД
   - Пример: `2024-01-01`

3. **Дата окончания** - формат ГГГГ-ММ-ДД
   - Пример: `2025-01-01`

4. **Сортировка**
   - `По дате` - сортировка по времени публикации
   - `По реакциям` - сортировка по количеству реакций
   - `По просмотрам` - сортировка по количеству просмотров

5. **Направление**
   - `По убыванию` - сначала новые/популярные
   - `По возрастанию` - сначала старые/менее популярные

**Опциональные поля:**

6. **Имя файла** - название для PDF (без расширения)
   - По умолчанию: `{канал}_{дата_начала}_{дата_окончания}.pdf`

### Шаг 3: Генерация PDF

Нажмите кнопку **"Сгенерировать PDF"**

Процесс:
1. ⏳ Сервер получает данные из Telegram (или демо-данные)
2. 🔄 Применяется сортировка
3. ✨ **Эмодзи нормализуются** (исправление бага!)
4. 📄 Генерируется PDF с корректными эмодзи
5. ⬇️ Файл автоматически скачивается

---

## 📋 API Endpoints

### GET `/`

**Описание:** Главная страница с формой

**Ответ:** HTML страница

### POST `/generate`

**Описание:** Генерация PDF отчёта

**Параметры (Form Data):**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `channel` | string | Да | Username канала |
| `date_from` | string | Да | Дата начала (ГГГГ-ММ-ДД) |
| `date_to` | string | Да | Дата окончания (ГГГГ-ММ-ДД) |
| `sort_type` | string | Да | Тип сортировки: `date`, `reactions`, `views` |
| `direction` | string | Да | Направление: `asc`, `desc` |
| `filename` | string | Нет | Имя файла (опционально) |

**Ответ:**
- **Успех:** PDF файл (application/pdf)
- **Ошибка:** HTML страница с сообщением об ошибке

**Пример запроса (curl):**

```bash
curl -X POST http://127.0.0.1:8000/generate \
  -F "channel=test_channel" \
  -F "date_from=2024-01-01" \
  -F "date_to=2025-01-01" \
  -F "sort_type=date" \
  -F "direction=desc" \
  -F "filename=my_report" \
  --output report.pdf
```

**Пример запроса (Python):**

```python
import requests

data = {
    "channel": "test_channel",
    "date_from": "2024-01-01",
    "date_to": "2025-01-01",
    "sort_type": "date",
    "direction": "desc",
    "filename": "my_report"
}

response = requests.post("http://127.0.0.1:8000/generate", data=data)

if response.status_code == 200:
    with open("report.pdf", "wb") as f:
        f.write(response.content)
    print("✅ PDF сохранён")
else:
    print(f"❌ Ошибка: {response.status_code}")
```

---

## 🔧 Конфигурация

### config.py

```python
# Telegram API credentials
API_ID = 28607273
API_HASH = "55165d607c1d26dca0d0844d437fde2d"

# Session файл
SESSION_NAME = "session"

# Демо-режим (True/False)
DEMO_MODE = False  # True для тестирования без Telegram
```

### Порт сервера

По умолчанию: **8000**

Для изменения порта отредактируйте [start_web.sh](start_web.sh):

```bash
uvicorn web_app:app --reload --host 127.0.0.1 --port 8080
```

---

## 🐛 Устранение проблем

### Проблема: "Address already in use"

**Причина:** Порт 8000 уже занят

**Решение:**
```bash
# Убить процесс на порту 8000
lsof -ti:8000 | xargs kill -9

# Или изменить порт в start_web.sh
```

### Проблема: "ModuleNotFoundError"

**Причина:** Не установлены зависимости

**Решение:**
```bash
source venv/bin/activate
pip install -r requirements.txt
pip install requests  # Для test_web_emoji.py
```

### Проблема: Эмодзи отображаются неправильно

**Проверьте:**

1. Файл `emoji_handler.py` присутствует
2. В `pdf_builder.py` импортированы функции из `emoji_handler`
3. Шрифты в папке `fonts/` доступны

**Тест:**
```bash
python3 test_emoji_comprehensive.py
```

### Проблема: "Session error" в продакшене

**Причина:** Нет авторизации Telegram

**Решение:**

1. Первый запуск - локально авторизуйтесь:
   ```bash
   python3 main.py
   # Введите номер телефона и код
   ```

2. Файл `session.session` будет создан

3. Скопируйте `session.session` в продакшен

4. Или используйте `DEMO_MODE = True` для тестирования

---

## 📊 Мониторинг

### Логи сервера

```bash
# Просмотр логов в реальном времени
tail -f server.log

# Последние 50 строк
tail -50 server.log
```

### Проверка статуса

```bash
# Проверка, запущен ли сервер
lsof -i :8000

# Тест доступности
curl http://127.0.0.1:8000/
```

### Метрики

Добавлены автоматически FastAPI:

- **Swagger UI:** http://127.0.0.1:8000/docs
- **ReDoc:** http://127.0.0.1:8000/redoc
- **OpenAPI JSON:** http://127.0.0.1:8000/openapi.json

---

## 🚢 Деплой в продакшен

### 1. Подготовка

```bash
# Установить зависимости
pip install -r requirements.txt

# Проверить наличие файлов
ls -l emoji_handler.py pdf_builder.py fonts/

# Установить демо-режим в False
# config.py: DEMO_MODE = False
```

### 2. Запуск с Gunicorn (рекомендуется)

```bash
# Установить Gunicorn
pip install gunicorn

# Запустить с 4 воркерами
gunicorn web_app:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

### 3. Systemd service (Linux)

Создайте `/etc/systemd/system/telegram-parser.service`:

```ini
[Unit]
Description=Telegram Channel Parser Web Service
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/project
Environment="PATH=/path/to/project/venv/bin"
ExecStart=/path/to/project/venv/bin/gunicorn web_app:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
Restart=always

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl daemon-reload
sudo systemctl start telegram-parser
sudo systemctl enable telegram-parser
```

### 4. Nginx reverse proxy (опционально)

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static {
        alias /path/to/project/static;
    }
}
```

---

## 🎯 Проверочный чек-лист

Перед использованием убедитесь:

- [ ] Веб-сервер запускается: `./start_web.sh`
- [ ] Главная страница доступна: http://127.0.0.1:8000
- [ ] Тест проходит успешно: `python3 test_web_emoji.py`
- [ ] Эмодзи отображаются корректно в PDF
- [ ] Демо-режим работает (если включён)
- [ ] Файлы `emoji_handler.py` и обновлённый `pdf_builder.py` на месте
- [ ] Шрифты в папке `fonts/` доступны

---

## 📚 Дополнительные ресурсы

- **Технический отчёт:** [EMOJI_FIX_REPORT.md](EMOJI_FIX_REPORT.md)
- **Руководство по эмодзи:** [EMOJI_USAGE_GUIDE.md](EMOJI_USAGE_GUIDE.md)
- **Краткий обзор:** [SUMMARY.md](SUMMARY.md)
- **Тесты:** [test_web_emoji.py](test_web_emoji.py)

---

## ✨ Особенности веб-версии

### С исправлением эмодзи

✅ **Все эмодзи отображаются корректно**
- Нет серых "ушек" над ❤️
- Составные эмодзи работают (🏳️‍🌈, 👨‍👩‍👧‍👦)
- Правильное выравнивание и пробелы

✅ **Автоматическая обработка**
- Нормализация при каждой генерации PDF
- Работает и в демо-режиме, и с реальными данными

✅ **Производительность**
- Быстрая генерация PDF
- Кэширование шрифтов
- Асинхронная обработка запросов
